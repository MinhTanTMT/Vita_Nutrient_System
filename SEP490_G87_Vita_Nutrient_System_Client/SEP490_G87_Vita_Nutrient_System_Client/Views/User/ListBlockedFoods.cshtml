@model List<FoodItem>
@{
    ViewData["Title"] = "Thực phẩm bị chặn";
}

<div class="container mt-5">
    <h2 class="text-center">Bộ sưu tập thực phẩm bị chặn</h2>

    <!-- Thanh tìm kiếm -->
    <div class="row mb-4">
        <div class="col-md-6 offset-md-3">
            <form method="get" class="input-group">
                <input type="text" name="search" value="@ViewBag.Search" class="form-control" placeholder="Tìm kiếm thực phẩm..." />
                <button type="submit" class="btn btn-primary">Tìm kiếm</button>
            </form>
        </div>
    </div>

    <!-- Bảng thực phẩm bị chặn -->
    <div class="table-responsive">
    <table class="table table-striped table-hover">
        <thead class="table-dark">
            <tr>
                <th>Hình ảnh</th>
                <th>Tên</th>
                <th>Mô tả</th>
                <th>Đánh giá</th>
                <th>Hành động</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var food in Model)
            {
                <tr>
                    <td>
                        <img src="@food.UrlImage" alt="Hình ảnh" class="rounded" width="50" height="50" />
                    </td>
                    <td>@food.Name</td>
                    <td>@food.Describe</td>
                    <td>@food.Rate</td>
                    <td>
                        <button type="button" class="btn btn-warning btn-sm" onclick="unblockFood(@food.FoodListId)">Bỏ chặn</button>
                    </td>
                </tr>
            }
        </tbody>
    </table>
    </div>

    <!-- Phân trang -->
    <nav aria-label="Điều hướng trang" class="d-flex justify-content-center">
        <ul class="pagination">
            @for (int i = 1; i <= ViewBag.TotalPages; i++)
            {
                <li class="page-item @(i == ViewBag.CurrentPage ? "active" : "")">
                    <a class="page-link" asp-action="Index" asp-route-page="@i" asp-route-pageSize="10" asp-route-search="@ViewBag.Search">@i</a>
                </li>
            }
        </ul>
    </nav>
</div>

<script>
    async function unblockFood(foodId) {
        try {
            // Gọi đến hàm Unblock trong controller thông qua URL được tạo bởi Razor
            console.log(foodId);
            const response = await fetch(`@Url.Action("Unblock", "User")?foodId=${foodId}`, {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                }
            });

            const result = await response.json();

            if (result.success) {
                alert("Bỏ chặn thành công.");
                location.reload();
            } else {
                alert(result.message || "Không thể bỏ chặn thực phẩm.");
            }
        } catch (error) {
            console.error("Lỗi khi bỏ chặn thực phẩm:", error);
            alert("Lỗi khi bỏ chặn thực phẩm.");
        }
    }
</script>

